/** Especificaciones de Clase Global de las Credenciales de Usuario */
import { Creds } from './creds';

describe('Creds', () => {
  it('should create an instance', () => {
    expect(new Creds()).toBeTruthy();
  });
});
